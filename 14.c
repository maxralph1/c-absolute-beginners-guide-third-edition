#include <stdio.h>

main()
{
    int ctr = 0;

    ctr = ctr + 1;
    printf("Counter is at %d.\n", ctr);
    ctr = ctr + 1;
    printf("Counter is at %d.\n", ctr);
    ctr = ctr + 1;
    printf("Counter is at %d.\n", ctr);
    ctr = ctr + 1;
    printf("Counter is at %d.\n", ctr);
    ctr = ctr + 1;
    printf("Counter is at %d.\n", ctr);
    ctr = ctr - 1;
    printf("Counter is at %d.\n", ctr);
    ctr = ctr - 1;
    printf("Counter is at %d.\n", ctr);
    ctr = ctr - 1;
    printf("Counter is at %d.\n", ctr);
    ctr = ctr - 1;
    printf("Counter is at %d.\n", ctr);

    return 0;
}